import styles from "@/styles/nowplaying.module.scss"

const PlayingIcon = () => {
  return (
    <div className={styles.icon}>
      <span />
      <span />
      <span />
    </div>
  )
}

export default PlayingIcon
